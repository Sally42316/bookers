<!DOCTYPE html>
<html lang="ja">
<head>
  <title>WebCampBackendNew</title>
  <meta charset="UTF-8"/>

</head>
<body>
<h2><strong>Editing Book</strong></h2>

<% if @book.errors.any? %>
  <%= @book.errors.count %> errors occurred
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>


<%= form_with model: @book do |f| %>

  <div class="edit-title">
  <p>Title</p>
  <%= f.text_field :title %>
  </div>
  
  <div class="edit-body">
  <p>Body</p>
  <%= f.text_area :body %>
  </div>
  
  <div class="edit-button">
  <%= f.submit'Update Book' %>
  </div>
  
<% end %>



    <table>
        <tbody>
            <tr>
                <td class="show-table-td1"><%= link_to "Show", book_path(@book.id) %></td>
                <td class="show-table-td2"><a href="/books">Back</a></td>
            </tr>
        </tbody>
    </table>

    </body>
</html>